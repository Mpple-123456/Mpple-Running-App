### 使用
#### 运行：
  ```bash
  pyhton main.py MRA（Mpple Running App）文件位置
  ```
### 编写并运行你自己的 MRA 程序
#### 新建一个文本文件，例如 my_program.mra，写入 MRA 代码：
  ```text
  set num age = 18
  out("我的年龄是：" + age)
  ```
#### 然后在终端运行
### 或是运行自带文件
